# MegaApply Functions

## Env
- SMTP_HOST
- SMTP_PORT (default 587)
- SMTP_USER
- SMTP_PASS
- MAIL_FROM

## Notes
- Uses RTDB path: /jobs for scraped jobs.
- Users are stored under /users/{uid}.
- Applications tracked under /applications/{uid}/{jobId}.
