// src/entry.js
// Railway/production entry point. It just imports your actual scraper.
// Locally, you can still run with: node -r dotenv/config src/entry.js
import { run } from "./index.js";

run();
